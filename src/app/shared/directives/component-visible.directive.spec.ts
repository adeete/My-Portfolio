import { ComponentVisibleDirective } from './component-visible.directive';

describe('ComponentVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
